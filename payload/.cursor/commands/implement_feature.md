# Implement Feature

## Input

Use the text the user typed after the command name as the request for this command.

- Treat it as the command input for the shared command doc.

## Instructions

Read and follow `~/ai-kb/commands/implement_feature.md` (canonical). If there is a conflict, prefer the shared doc.
